TypeScript
    HelloTS
        package.js (node, npm)
        tsconfig.json (TypeScript)
        .babelrc (Babel)
        src (pliki źródłowe)
        lib (pliki wynikowe)

Zadanie:
Klasa domenowe TS + mini api CRUD, wykorzystać interface, dziedziczenie,