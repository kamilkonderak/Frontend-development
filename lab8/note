1. Interakcja między komponentami 
    - "view" <-> "model"
    - data binding
    () Event - API między komponentami
    <button (click) = "parentMethod()">
2. Formularze

Zadanie:

Wyszukiwarka, wyszukiwanie z objektów w danej dziedzinie (Reactive Form)